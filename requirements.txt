# Core runtime (Streamlit app + model inference)
streamlit>=1.30,<2.0
pandas>=1.5,<3.0
numpy>=1.23,<3.0
scikit-learn>=1.2,<2.0
joblib>=1.2,<2.0
tensorflow>=2.13,<3.0

# Optional but useful (only if we actually use .env in scripts/config)
# python-dotenv>=1.0,<2.0

